# Authoritative frontier priority note v0.1

Date: 2026-06-07 UTC
Updated: 2026-06-08T11:41:46+02:00
Lane: `limen-regulator-court-enforcement-scout`

## Why this artifact exists

The authoritative package is no longer blocked by total absence of useful work; it is blocked by route choice. The highest-value next moves are no longer interchangeable. Some routes would add a new official-theme exemplar, while others would only improve support depth for already seeded rows.

This note turns that distinction into a small reviewer-safe priority contract.

## Frontier split

1. `LIMEN-000002` and `LIMEN-000005` are now the two highest-value authoritative routes.
   - `LIMEN-000002` remains the clearest open-court-surface gap: the civil-action number is stable, but no open complaint, decree PDF, docket entry, or judicial order text is yet captured.
   - `LIMEN-000005` remains the clearest normalization gap: the indictment package exists, but the numeric suffix after `25 Cr.` is still unread.
   - These routes improve evidence depth and citation precision more than another diversification memo would.

2. `AUTHTHRESH-005` is now the strongest geography-diversification route.
   - The live package already has bounded Slovenia, Denmark, and France rows, but still lacks a document-grade or court-facing non-U.S./non-UK authority chain.
   - The next meaningful move is one materially stronger official companion: a case-specific CNIL companion, a Danish case-specific DPIA/procurement/evaluation companion, or an ORION partner/project document that deepens authority depth.
   - This route matters because it changes reviewer-facing authority geography without flattening governance-only or warning-page material into stronger proof than it supports.

3. `QUEUE-HUMAN-AS-AI-001` remains an explicit high-value conceptual blocker.
   - Nate is still accusation-stage only, while DoNotPay and Workado now already sit inside the shared authoritative core as bounded FTC rows with different subtype roles.
   - Those FTC rows diversify official AI-claim support, but they do not retire the hidden-human final-authority queue.
   - A later regulator-final or court-facing hidden-human row would change the package more than another FTC packaging pass.

4. `LMWCS-20260606-001` remains a valuable later underrepresented-language corroboration route.
   - Finland still needs one official police, court, ministry, or administrative-process companion before it can move beyond broadcaster-level support.
   - It should stay visible, but behind the court-depth, non-U.S./non-UK authority-depth, and hidden-human threshold rows.

5. `LIMEN-000010` and `LIMEN-000011` are no longer frontier promotions.
   - DoNotPay and Workado are already promoted into the shared authoritative core.
   - They now belong in reuse/control logic, not in live route selection, unless a shared-consumer drift or a deliberate denominator/core-package rewrite appears.

## Paper/thesis use

- The register can feed a dashboard priority widget, methods/limitations prose, or a short thesis planning table showing the difference between `depth upgrades`, `geography-diversification thresholds`, `hidden-human final-authority gaps`, and `later corroboration routes`.
- The main interpretive value is that LIMEN can now say why a next move was chosen, not just which URL was tried.

## Conservative reading rule

This artifact does not promote any new canonical authoritative row by itself.
It only ranks bounded next routes using already captured public/open local evidence.
