# LIMEN crosswalk lane cycle note

Date: 2026-06-07T06:31:38Z
Lane: `limen-aiid-oecd-avid-crosswalk`
Project: `limen-ai-edge-case-atlas`

## Action

Re-read stop controls, the hostile-reviewer sprint, the LIMEN goal card, and the current six-source crosswalk package after confirming there was still no active `/srv/tyche/STOP` or project `STOP` file.

Chose bounded interoperability verification over comparator expansion.

## Artifacts touched

- `results/crosswalks/source-crosswalk-v0.1.tsv`
- `results/crosswalks/field-map-appendix-v0.1.tsv`
- `methods/crosswalk-method-note-v0.1.md`
- `results/crosswalks/status.md`
- `journal.md`

## Paper-readiness delta

The crosswalk package now has one stronger machine-readable AIID anchor without crossing into archive mirroring: the public snapshot stream supports collection metadata plus first-record keys (`_id`, `incident_id`, `language`, `created_at`, `title`, `description`). MIT AI Risk is also more defensible as a licensed taxonomy bridge because the package now distinguishes clearly between visible JSON-LD dataset metadata and still-gated download routes.

## Claims verified, rewritten, or dropped

Verified:
- AIID public snapshots still expose dated public `.tar.bz2` archives and the streamed archive still contains `translations/incidents.bson`, `translations/reports.bson`, and `translations/incidents.metadata.json`.
- A bounded first-record AIID BSON read exposes `_id`, `incident_id`, `language`, `created_at`, `title`, and `description`.
- MIT AI Risk `/risks` still exposes JSON-LD dataset metadata with CC BY 4.0 license, named `hasPart` taxonomy components, and advertised Google Sheets / OneDrive distribution links.
- Those MIT AI Risk distribution links still resolve to Google and Microsoft login flows rather than an anonymous export payload.

Rewritten more cautiously:
- AIID is now framed as first-record-key verified rather than just metadata-index verified, but still not as bulk/API-cleared import.
- MIT AI Risk remains a taxonomy/definition bridge and not a verified open bulk-download source.

## Remaining blocker

The package still needs one additional bounded AIID field-family verification step and/or one directly visible MIT AI Risk concept-level label/object surface before stronger interoperability language is justified.

## Suggested next route

Exploit the current six-source package with a tiny join prototype or one more bounded field-surface verification step rather than add more comparator families.
