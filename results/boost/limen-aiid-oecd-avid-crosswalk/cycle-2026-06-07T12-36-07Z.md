# limen-aiid-oecd-avid-crosswalk cycle note

Date: 2026-06-07T12:36:07Z
Project: `limen-ai-edge-case-atlas`
Lane: `limen-aiid-oecd-avid-crosswalk`

## Route chosen

Chose live-source revalidation and denominator repair over new comparator expansion. The package already had the required six-source crosswalk, but two reviewer-visible drifts remained inside the existing artifacts: one stale `16`-row denominator in the seed-join control table and one stale AIID exact-field implication around taxonomy metadata.

## Bounded actions completed

1. Re-checked STOP-file absence, the hostile-reviewer sprint, and the LIMEN goal card.
2. Re-read the live crosswalk package files.
3. Re-checked bounded public access/reuse posture for AIID, OECD, AVID, MIT AI Risk, CSET, and MITRE ATLAS.
4. Refreshed `results/crosswalks/source-crosswalk-v0.1.tsv` with tighter AIID wording and fresh provenance notes.
5. Repaired `results/crosswalks/seed-import-join-prototype-v0.1.tsv` so the package-wide comparator denominator now reads `18` local rows.
6. Rewrote `methods/crosswalk-method-note-v0.1.md` and `results/crosswalks/status.md` to reflect the current verified package state.

## Paper-readiness delta

- The crosswalk package now has one cleaner reviewer-safe story about what is verified today.
- AIID exact-field wording is less vulnerable to overread.
- Comparator-slot counts now match the live corpus slice used by the package.
- The package remains small and interpretable enough to feed a source-readiness board, occupancy panel, and security/taxonomy split view.

## Claims verified, rewritten, or dropped

Verified:
- AIID cite page and page-data JSON remain public.
- OECD detail page, detail JSON, and vocab endpoints remain public.
- AVID remains MIT-licensed with raw JSON.
- MIT AI Risk retains CC BY 4.0 metadata on `/risks`.
- CSET remains public but citation/manual-only.
- MITRE ATLAS remains Apache-2.0 licensed and distributed as structured YAML.

Rewritten:
- AIID exact-field ceiling now excludes taxonomy claims not re-verified in this cycle.
- Seed-join denominator language now states `18` local rows rather than `16`.

Dropped/avoided:
- no new source family;
- no bulk mirroring;
- no widened reuse claim;
- no new row-level join claim.

## Remaining blocker

The package still needs one threshold-changing row-level join improvement before stronger interoperability prose is safe.

## Suggested next move

Prefer one of:
- a second canonical AVID join;
- one defensible MITRE ATLAS case-study ID;
- one bounded same-event AIID/OECD pilot only if identity can be shown conservatively.
