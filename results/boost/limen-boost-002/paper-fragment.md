# Shard 002 paper fragment

Generated: 2026-06-07T07:06:07Z
Lane: `limen-boost-002`
Sprint: `20260607-hostile-reviewer-pass`

## Reviewer-safe shard claim

LIMEN's six-source comparator package is now strong enough to support a claim-ceiling paragraph about asymmetric interoperability, not a claim that external frameworks uniformly validate the atlas. The strongest current bridges are two structured public security-oriented sources with exact identifiers and reusable machine-readable payloads (AVID and MITRE ATLAS), two bounded public incident comparators whose detail surfaces are verified but still rights- or payload-limited (OECD AI Incidents Monitor and AIID), one concept-level licensed taxonomy surface with a named-anchor appendix, one imported workbook slice, and ten shared local bridge rows but still no event-level joins (MIT AI Risk), and one manual citation-only harm overlay (CSET). This means LIMEN can now defend why some crosswalks belong in machine-readable appendix tables while others should remain metadata-first or interpretive only.

## Exact support added this cycle

- `comparator-claim-ceiling-matrix.tsv` converts the six comparator families into one reviewer-facing table with control class, verification state, exact identifier surface, safe role, forbidden overread, and next bounded upgrade.
- The matrix makes the current asymmetry legible in one place: exactly two rows are ready for structured seed-import language now (`avid`, `mitre_atlas`), two rows are bounded-public-comparator surfaces only (`oecd_aim_incidents`, `aiid`), one row remains a ten-bridge taxonomy-bridge program surface (`mit_ai_risk_repo`), and one row remains manual citation support only (`cset_ai_harm`).
- The matrix also preserves a key methods boundary: external appearance or identifier availability is evidence of interoperability opportunity, not automatic promotion of LIMEN evidence tier, legality framing, or same-event identity.

## Paper/thesis use

- Methods/data paper: use shard 002 as the compact interoperability-governance paragraph explaining why LIMEN stores crosswalk slots for six source families but exploits them at different depths.
- Thesis chapter on evidence infrastructure: use the matrix to show how public-source observatories need explicit claim ceilings across machine-readable, metadata-first, and conceptual bridges.
- Appendix/table route: the matrix can back a small "comparator role and ceiling" table alongside the existing field-map appendix and security crosswalk anchor matrix.

## Main limitation

The package now has one join-safe bounded same-event overlap example for each of AIID and OECD (`LIMEN-000008 -> 628`; `LIMEN-000001 -> 2023-03-31-960d`) and a ten-bridge MIT AI Risk concept layer, but still has no second overlap in either incident family, no MIT AI Risk event-level joins, and no basis to collapse security-technique structures, incident monitors, and conceptual taxonomies into one undifferentiated external-validation layer.

## Next smallest publishability move

If no stale shared comparator surface remains, pursue only one threshold-changing upgrade next: either a third canonical AVID direct join, a first defensible direct MITRE ATLAS case-study ID, or a bounded MIT AI Risk eleventh-bridge or second-slice promotion. Keep CSET on the manual-overlay frontier and keep MIT AI Risk explicitly category-level unless a concrete manuscript or dashboard surface needs the already-materialized ten-bridge package.
