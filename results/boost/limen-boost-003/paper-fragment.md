# Paper fragment — limen-boost-003

Shard-003 should now be narrated as a threshold-aware authority-route control surface, not as a flat regulator/court inventory. The live front door is `results/boost/limen-boost-003/regulator-court-lead-board.tsv`, which keeps nine route classes separate: the shared `11`-row authoritative core front door; the `LIMEN-000002` open-court-text blocker; the `LIMEN-000005` caption-normalization blocker; the explicit `QUEUE-HUMAN-AS-AI-001` final-authority gap; two bounded FTC reuse rows (`LIMEN-000013` reversibility chronology and `LIMEN-000014` court-settlement sidecar); and three non-U.S./non-UK diversification rows (`LIMEN-000015`, `LIMEN-000012`, and `LIMEN-000009_plus_non_us_follow_through`) that still sit below document-grade enforcement or court-facing depth.

For manuscript and thesis reuse, the package-state split now matters more than the raw row count. Figure 8 / package-state prose can say that the shared authoritative core currently spans `11` rows at `United States=7 / United Kingdom=2 / Italy=1 / Slovenia=1`, while the default Table 1A document-depth facet remains the narrower `10`-row surface because the Slovenian official project-page row is bounded follow-through rather than document-grade regulator/court text. The French CNIL warning-page row remains a reusable non-core sidecar outside that denominator, and the hidden-human final-authority queue remains open even after DoNotPay and Workado entered the shared authoritative core.

This route split improves reviewer safety because it prevents later prose from overstating what the authoritative package solved. `LIMEN-000002` still needs one genuinely open court-facing complaint, decree, docket entry, or judicial order; `LIMEN-000005` still needs a materially better public caption source before exact caption metadata can be promoted; and the non-U.S./non-UK rows still support diversification and bounded governance/official-follow-through discussion rather than court-depth closure. By contrast, `LIMEN-000013` and `LIMEN-000014` are now useful as bounded chronology/settlement sidecars precisely because their reuse no longer has to pretend to be a threshold-closing event.
