# Public-sector comparator freeze note

- Timestamp (UTC): 2026-06-07T03:25:45Z
- Lane: `limen-boost-006`
- Sprint route: hostile-reviewer pass via bounded freeze note rather than unsupported public-sector expansion.

## Why this artifact exists

`next.md` allowed two safe routes for `sf09`: add a second jurisdictional public-sector register/procurement comparator, or write a bounded freeze note if the local package still cannot support broader comparative language. After re-reading the current LIMEN public-sector package, the safer route is now to freeze the family claim ceiling explicitly.

## Bounded family statement

The current local public-sector comparison supports:

- one Dutch public algorithm-register exemplar (`psdc-004`);
- one Slovenian official project-page anchor (`psdc-001`);
- two UK regulator-document anchors (`psdc-002`, `psdc-003`).

This is enough for a methods/results claim about disclosure-surface heterogeneity and reconstruction ceilings. It is not enough for a broader procurement-transparency package, buyer-side traceability comparison, or country-balanced public-sector disclosure generalization.

## Structured support

Use `results/boost/limen-boost-006/comparator-freeze-matrix.tsv` to see exactly why each current row remains in the package and why it still cannot be promoted into a stronger register/procurement family claim.

## Paper/thesis use

- hostile-reviewer-safe limitation note for the first LIMEN article package;
- methods paragraph explaining why surface-class comparison stops at one register exemplar in the current corpus;
- dashboard caption constraint for the public-sector disclosure panel.

## Visualization/dashboard hook

- Add a `promotion_status` or `family_claim_ceiling` tooltip to the public-sector disclosure panel.
- Distinguish `singleton_register_exemplar` from `regulator_depth_anchor` and `project_page_anchor` in captions or side notes.

## Next smallest publishability move

Do not expand weak public-sector leads laterally. Either capture one second non-Dutch public register/procurement/buyer-side comparator that fits the same columns, or keep the family frozen at the current claim ceiling.
