# Dashboard hook — limen-boost-007

Generated: 2026-06-07T04:26:50Z

Primary artifact
- `results/boost/limen-boost-007/evidence-layer-funnel.tsv`

Why this helps publication
- Converts shard 007's current synthetic-voice package into a reviewer-legible funnel: blocked state-origin layer -> public institutional response -> document-grade warning posture -> federal policy proposal -> federal rule anchor -> federal enforcement bridge.
- Makes the hostile-reviewer question explicit: which layers have exact quoted text, which layers are still posture-stage only, and which layer is still blocked from this environment.

Suggested figure/table
- Figure title: `Evidence-layer funnel for the synthetic-voice provenance-confusion cluster`.
- Plot each `stage_key` as one ordered step with badges for `exact_quote_available`, `evidence_tier`, and `authority_tier`.
- Use color to distinguish source roles rather than treating all rows as incident facts.

Machine-readable fields the dashboard should consume
- `cluster_or_anchor_id`
- `stage_order`
- `stage_key`
- `procedural_posture`
- `source_depth`
- `exact_quote_available`
- `evidence_tier`
- `authority_tier`
- `reviewer_safe_function`
- `claim_ceiling_note`
- `next_stronger_source_or_move`

Current reviewer-safe interpretation
- There are 5 direct authoritative rows in the current funnel and 4 of the 6 rows expose exact quote availability.
- The remaining blocked layer count is 1; that row documents source-surface friction, not direct state-case text.
- The funnel is strongest as a methods/results visualization showing how LIMEN separates evidence roles inside one cluster instead of flattening them into one incident count.

What this figure should not imply
- It should not imply final liability, chronology completeness, prevalence of AI impersonation, or success/failure of the underlying policy responses.
- Proposal-stage and allegation-stage rows must remain visibly labeled as such.

Next smallest publishability move
- Add one accessible state-origin document or a second document-grade cluster so the funnel can be compared across at least two shard-007 case families.
