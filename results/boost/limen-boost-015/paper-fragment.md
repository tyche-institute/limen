# Paper fragment

Shard 015 now leaves a route governor instead of another free-form frontier summary. `authoritative-route-governor.tsv` should now be read through the current split between the 11-row authoritative package-state core and the narrower 10-row default Table 1A document-depth facet. Under that live split, DoNotPay remains the best first denominator-changing non-finance official-text promotion for `LIMEN-C-005` / Table 1C, Workado is now a package-state/core subtype note but still not part of the default Table 1A facet, `LIMEN-000002` remains the highest-value court-depth repair for `LIMEN-C-010` / Table 1A, `LIMEN-000005` remains primarily a citation-precision route, the Danish regulator row remains a breadth upgrade for `LIMEN-C-012`, Finland remains queue-only until an official companion appears, and notice-only surfaces remain explicit non-substitutes.

That matters for hostile-reviewer pass because shard 003 no longer has to be narrated as one vague stronger-source backlog. The governor separates denominator-changing promotions from court-depth repair, citation-normalization work, rare-language breadth expansion, and negative-result holds while preserving current package scope: Figure 8 now travels as the 11-row authoritative concentration panel (`United States=7`, `United Kingdom=2`, `Italy=1`, `Slovenia=1`), whereas Table 1A remains the narrower 10-row default facet (`7` direct-document, `1` bounded official-project-page follow-through, `1` partial-normalization, `1` summary-only). A compliant DoNotPay promotion diversified non-finance official-text support inside the core without solving the hidden-human final-authority gap; Workado widened the package-state core only as a narrower detector-governance official subtype and still should not be flattened into the default Table 1A denominator; one genuine court-facing `LIMEN-000002` document would strengthen the weakest authoritative row without making the package geographically balanced; and one Danish regulator-originating row would broaden governance visibility without becoming a harm or illegality exemplar.

The route governor also hardens the package against false progress. It states directly that company notices, repo issues, milestone pages, and same-host OCR reruns do not count as authoritative diversification or court-depth improvement. Until one threshold-changing route clears, the safe package remains bounded: `LIMEN-000002` is still summary-only, `LIMEN-000005` is still normalization-limited, Finland is still non-official, and notice-only materials still belong in limitations logic rather than in the authoritative denominator.
