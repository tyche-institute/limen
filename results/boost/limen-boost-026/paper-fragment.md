# Paper fragment — authoritative comparator asymmetry

Date: 2026-06-08
Lane: limen-boost-026
Artifact basis: `authoritative-comparator-asymmetry-board.tsv` (14 authoritative-core rows), reused under the hostile-reviewer sprint to produce manuscript-ready bounded prose rather than another retrieval pass.

## Results paragraph draft

Within the current 14-row authoritative LIMEN core, exact external comparator overlap remains deliberately narrow. Two rows carry one bounded exact same-event comparator pilot (2/14, 14.3%), two more rows are retained as aggregate sidecar-only matches because each public external record collapses multiple local SEC rows (2/14, 14.3%), and the remaining ten authoritative rows stay comparator-zero under current one-row-to-one-row rules (10/14, 71.4%). This should be read as a current interoperability-asymmetry result inside the package, not as proof that no external record exists elsewhere for those official cases.

## Limitations paragraph draft

The geography of this asymmetry is also uneven. `United States / en` contributes 9 of the 14 authoritative rows, including the only non-Italian exact pilot and both aggregate sidecar-only SEC rows, yet it still contains six comparator-zero authoritative records. By contrast, the current `United Kingdom / en` (2 rows), `Slovenia / sl` (1 row), and `Denmark / da` (1 row) slices remain entirely comparator-zero under the same bounded rules. This pattern supports a cautious country/language limitations claim about visible comparator occupancy in the present package, but it does not justify broader claims about international comparator absence or dataset completeness.

## Figure/table caption draft

Suggested caption for an authoritative-core comparator-gap inset: “Authoritative LIMEN rows currently split into two exact pilots, two aggregate sidecar-only rows, and ten comparator-zero rows. The package therefore supports a bounded interoperability demonstration plus a larger evidence-asymmetry finding: strong official documentation often remains unmatched by row-exact external comparator records.”

## Reviewer-safe interpretation notes

- Exact pilots stay exemplar-level only; they do not license a package-wide external-validation claim.
- Aggregate SEC collisions remain visible methods material, but not denominator-bearing overlap growth.
- Comparator-zero official rows remain publication-useful as evidence of cross-database sparsity under current rules.

## Route note for future work

If shard-002 reopens substantively, the next honest threshold-changing order remains: one additional canonical AVID direct join first, one defensible direct MITRE ATLAS case-study identifier second, then a second exact AIID or OECD overlap. Governance/public-sector comparator probes are still the most plausible reopening route for the non-U.S. comparator-zero official rows, but only if a bounded same-event or official-record payoff is visible.

## Evidence anchors

- `results/boost/limen-boost-026/authoritative-comparator-asymmetry-board.tsv`
- `results/boost/limen-boost-026/authoritative-comparator-gap-panel.tsv`
- `results/boost/limen-boost-050/comparator-threshold-impact-matrix.tsv`
