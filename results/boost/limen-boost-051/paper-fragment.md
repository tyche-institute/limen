# Paper fragment

Shard 051 now adds one manuscript-ready bridge for a recurring hostile-reviewer problem: LIMEN's strongest official-text diversification route still crosses shard 009 and shard 003, but the live authoritative package has moved beyond the older `8-row Table 1A / 9-row Figure 8` shorthand and should no longer be narrated that way. The current reviewer-safe baseline is now split explicitly. Table 1A is a `10-row` authoritative document-depth facet with `7` direct-document rows, `1` bounded ORION official-project-page follow-through row, `1` Nate charging-stage partial-document row, and `1` EEOC summary-only escalation row. Figure 8 and the shared authoritative package-state core are broader at `11` rows with jurisdiction split `United States=7 / United Kingdom=2 / Italy=1 / Slovenia=1`; that broader core includes both DoNotPay and Workado, while Denmark and the French CNIL row remain bounded sidecars outside the count-bearing panel.

That split matters because the authoritative package is already deep enough for selective official-text reuse, but still heterogeneous enough that route drift would weaken the paper. DoNotPay and Workado now widen the official non-finance and detector-governance story inside the shared core; they do not repair `LIMEN-000002`'s absent court-facing text, `LIMEN-000005`'s caption-normalization blocker, or the package's non-U.S./non-UK document-grade concentration. ORION improves geography visibility and multilingual/public-sector follow-through discussion, but it still sits below document-grade regulator/court depth in the default Table 1A facet. Denmark and the French CNIL row remain useful bounded sidecars for public-sector governance and non-English biometric-governance comparison, not silent denominator growth.

For the first article package, this bridge is best used as a bounded methods/results paragraph and reviewer-response control note. It supports a narrow claim that LIMEN can already contrast settlement-order anchors, accusation-stage and summary-only court-gap material, bounded non-U.S./non-UK follow-through, and official non-finance FTC diversification without collapsing them into one generic "stronger evidence" story. It does not support broader claims about cross-sector prevalence, hidden-human closure, court-verified facts for Nate or iTutorGroup, company-notice equivalence to regulator/court/final-order text, or internationally balanced authoritative coverage.