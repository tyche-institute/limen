# LIMEN continuous experiments cycle note

Timestamp (UTC): 2026-06-07T06:50:17Z
Lane: `limen-continuous-experiments`
Sprint: `20260607-hostile-reviewer-pass`

## Why this route

The sprint priority had shifted from corpus growth toward reviewer-facing package coherence. The smallest useful move was to reduce drift between boost-local dashboard assets and shared manuscript-facing references, while keeping denominator and caution logic explicit.

## What changed

- Created `results/dashboard-paper/shared-export-promotion-register-v0.1.tsv` as a machine-readable routing register for ten reviewer-safe dashboard exports.
- Patched `draft/preprint.md` so the shared package framing now names the refreshed 21-lineage funnel, the 7/7/2/5 authority-balance panel, and the Helsinki same-buyer procurement-companion ceiling.
- Attempted direct promotion into `results/dashboard/`, but this host blocked writes to that path during the cycle; the negative result is recorded in the register rather than hidden.

## Paper-readiness delta

- Manuscript framing now reflects the current authority-balance and procurement-companion logic instead of relying only on older staged language.
- Dashboard/paper consumers now have one register showing which current source file should be cited for each intended shared export, which denominator each file carries, and what caution remains attached.
- The blocked shared-path write is now explicit negative infrastructure evidence, which reduces the risk of silent path drift or undocumented UI/manuscript assumptions.

## Claims and boundaries

- No new case, legal conclusion, prevalence claim, or duplicate merge was added.
- The Helsinki row remains bounded at register entry plus same-buyer procurement companion evidence; it is not promoted to exact page-specific contract proof.
- The authority-balance panel remains a reviewer-control surface, not a completeness claim.

## Remaining blocker

- `results/dashboard/` is currently unwritable from this lane on this host, so the project still lacks a truly shared dashboard-export directory even though the export candidates are ready.

## Next smallest move

1. Either fix write access for `results/dashboard/` or keep `results/dashboard-paper/shared-export-promotion-register-v0.1.tsv` as the temporary canonical routing layer.
2. Once path promotion is possible, materialize the shared exports and update dashboard/manuscript references to those paths directly.
3. After the path issue is resolved, return to source-family hardening in sprint order: `sf08`, `sf09`, `sf11`.
