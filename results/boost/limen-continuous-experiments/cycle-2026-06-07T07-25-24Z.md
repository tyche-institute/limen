# LIMEN continuous experiments cycle note

Timestamp (UTC): 2026-06-07T07:25:24Z
Lane: `limen-continuous-experiments`
Sprint: `20260607-hostile-reviewer-pass`

## Why this route

The sprint and local goal card both favored hostile-reviewer package hardening over new collection. The smallest remaining publishability move was to patch the shared preprint surface so multilingual rows and intended dashboard exports inherit the same explicit control logic already adopted in the dashboard spec and article architecture.

## What changed

- Patched `draft/preprint.md` so the dashboard/package fragment now cites `results/multilingual/multilingual-publication-routing-matrix-v0.1.tsv` and `results/dashboard-paper/shared-export-promotion-register-v0.1.tsv` directly.
- Added explicit caption discipline to the preprint layer: multilingual geography/table surfaces must carry six-of-six translation-review-required caution, and intended `results/dashboard/` references must route through the export-promotion register while the shared path remains blocked.
- Updated `next.md` so the project now treats the preprint, dashboard spec, and article architecture as aligned shared control surfaces and defers new harvesting until any remaining shared caption/table omissions are fixed.

## Paper-readiness delta

- Shared manuscript prose no longer relies on implicit multilingual counts or implicit dashboard-export availability.
- The preprint now matches the shared `1 / 2 / 3` multilingual publication-bucket logic already used in dashboard/article artifacts.
- The blocked `results/dashboard/` promotion state is now visible in manuscript-facing prose, reducing the risk that a reviewer or future editor mistakes intended export paths for materialized shared files.

## Claims and boundaries

- No multilingual row was promoted.
- No unresolved row was reclassified.
- No dashboard export was claimed to exist under `results/dashboard/`.
- No new legal, prevalence, procurement, or incident-strength claim was added.

## Remaining blocker

A final audit may still find another shared caption/table surface that omits either the multilingual routing matrix or the export-promotion register. If that audit is clean, the next bounded move is one clearly compliant unresolved-row probe, with `Kaleva` still the safest candidate.

## Next smallest move

1. Check for any remaining shared caption/table surface that still omits either control artifact and patch it first.
2. If the shared control layer is fully adopted, attempt at most one compliant `Kaleva` route test.
3. Keep Lithuanian and Estonian rows frozen in queue-only status unless a new clearly compliant route appears.
