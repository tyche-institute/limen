# LIMEN continuous experiments cycle note

Timestamp (UTC): 2026-06-07T12:49:19Z
Lane: `limen-continuous-experiments`
Sprint: `20260607-hostile-reviewer-pass`

## Why this route

The strongest remaining reviewer-visible gap for the repeated lane was no longer missing caption control for Figure 3/4. Those controls now exist. The higher-value defect was stale shared reuse status around Figure 7 / `LIMEN-VIS-010`: several shared registry surfaces still treated the authority-balance panel as if filesystem write-enable were the only blocker, even though the live duplicate ledger already shows that the current 21-lineage panel is a bounded legacy single-state export below a 23-lineage overlap-aware ledger.

## What changed

- Re-read `results/boost/limen-boost-059/multistate-shared-surface-drift.tsv`, `results/boost/limen-boost-035/authority-balance-multistate-repair.tsv`, `results/boost/limen-boost-011/singleton-multistate-propagation.tsv`, and the live shared dashboard-paper registries.
- Patched `results/dashboard-paper/figure-table-priority-register-v0.1.tsv` so `LIMEN-VIS-010` is no longer treated as simply `ready_now`; it now carries explicit legacy-single-state caution.
- Patched `results/dashboard-paper/shared-export-promotion-register-v0.1.tsv:exp-008` so promotion is blocked by schema/denominator repair as well as blocked shared-path writes.
- Patched `results/dashboard-paper/caption-control-register-v0.1.tsv:CCR-014` so Figure 7 caption reuse now preserves the omitted singleton-plus-collision warning for `LIMEN-000008` and `LIMEN-000009`.

## Paper-readiness delta

- LIMEN's shared dashboard/manuscript control stack now makes the Figure 7 denominator risk reviewer-visible instead of hiding it behind a generic not-yet-promoted state.
- The project is less likely to overclaim that a writable dashboard path alone would make Figure 7 publication-safe.
- Figure 7 can still be reused, but only as a bounded legacy single-state panel until a multistate-safe export or sidecar validator exists.

## Claims and boundaries

- No source family was promoted.
- No case row changed evidence tier, authority state, or duplicate verdict.
- No new public source was fetched.
- The cycle only exploited already captured local/public-open artifacts and tightened reuse discipline.

## Remaining blocker

The underlying blocker is still structural: the Figure 7 / Figure 5 family needs one multistate-safe export or explicit sidecar validator so singleton review and identifier-collision protection can travel together for `LIMEN-000008` and `LIMEN-000009`.

## Next smallest move

1. Write a compact machine-readable validator or experiment request for the Figure 5 / Figure 7 multistate export contract.
2. After that validator exists, patch the remaining shared prose surfaces that still narrate the 21-lineage panel without the new legacy-state warning.
3. Do not treat dashboard write-enable alone as sufficient for `exp-008` promotion.
