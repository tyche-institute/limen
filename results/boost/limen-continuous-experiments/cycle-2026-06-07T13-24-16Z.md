# LIMEN continuous experiments cycle note

Cycle timestamp (UTC): 2026-06-07T13:24:16Z
Lane id: `limen-continuous-experiments`
Sprint: `20260607-hostile-reviewer-pass`

## Why this experiment

The highest-value bounded gap remained Figure 2 / Table 2 synchronization. The live taxonomy package already records the corrected `31 visible rows / 23 unique lineages` denominator and the corrected `11 authority-backed / 1 candidate-only / 3 zero-seed` support split, but the staged forge heatmap still exposes an older row schema and stale category-support state.

## What I did

- Re-checked STOP status, sprint directive, project goal card, and `next.md`.
- Compared `results/boost/limen-dashboard-paper-forge/taxonomy-heatmap.tsv` row-by-row against `results/boost/limen-boost-046/taxonomy-live-refresh-bundle.tsv`.
- Wrote `results/boost/limen-continuous-experiments/taxonomy-live-regeneration-delta-v0.1.tsv` as a machine-readable regeneration sidecar for all 15 taxonomy rows.

## Durable artifact created

- `results/boost/limen-continuous-experiments/taxonomy-live-regeneration-delta-v0.1.tsv`

## Main findings

- All 15 rows now have a live sidecar record tying the staged heatmap row to the live duplicate-controlled lineage contract and fragility flags.
- The most substantive state change remains `deepfake_or_synthetic_identity`: staged row = `2` seeded / `0` authoritative / `candidate_only_seeded`; live row = `3` lineages / `1` direct authority / `authoritatively_seeded`.
- Overlay-ready families stay unchanged in membership but not denominator wording: security remains `7/23`, AI-misstatement `3/23`, biometric comparator `2/23`.
- Negative result: I did not promote or overwrite the staged forge heatmap itself in this cycle, because the live refresh bundle carries sidecar-only fragility and lineage fields that would be lossy if silently flattened back into the older staged schema.

## Paper-readiness delta

LIMEN now has one lane-local, machine-readable regeneration contract that a later patch cycle can use to refresh Figure 2 / Table 2 shared surfaces without recomputing row logic from prose. This reduces reviewer-visible drift between the staged heatmap, the live taxonomy bundle, and shared caption/registry surfaces.

## Remaining blocker

A future synchronized patch still needs to decide whether the staged forge heatmap should (a) keep its old schema and only repair the stale counts/states, or (b) absorb a new fragility-aware field set before downstream shared-surface promotion.

## Next smallest publishability move

Use this sidecar together with `results/boost/limen-boost-022/taxonomy-refresh-fragility-propagation.tsv` to patch `LIMEN-VIS-002`, the dashboard spec, and manuscript Figure 2 / Table 2 prose in one synchronized bundle after the staged heatmap schema decision is made.
