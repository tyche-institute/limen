# LIMEN continuous experiments cycle note

Cycle timestamp (UTC): 2026-06-07T14:04:54Z
Lane id: `limen-continuous-experiments`
Sprint: `20260607-hostile-reviewer-pass`

## Why this experiment

The next smallest paper-readiness move under the sprint and goal-card rules was not another generic Route-B reminder. The live LIMEN source-map stack already says `sf11` is the third blocker, but the lane still lacked one compact selector that tells a later cycle which exact `sf11` route changes package strength fastest and which tempting near-misses must stay negative evidence.

## What I did

- Re-checked STOP status, sprint directive, project goal card, `next.md`, and the lane-local Route-B priority matrix.
- Re-read the live `sf11` route stack: `results/boost/limen-boost-040/security-route-decision-register.tsv`, `results/security/security-agentic-gap-closure-register-v0.1.tsv`, `results/boost/limen-boost-052/security-zero-state-closure-ladder.tsv`, `results/security/security-agentic-peer-review-frontier-v0.1.tsv`, and `results/security/security-agentic-scholarly-screening-v0.1.tsv`.
- Wrote `results/boost/limen-continuous-experiments/sf11-zero-state-next-route-selector-v0.1.tsv` as a lane-local selector for the four highest-value bounded `sf11` moves.

## Durable artifact created

- `results/boost/limen-continuous-experiments/sf11-zero-state-next-route-selector-v0.1.tsv`
- `results/boost/limen-continuous-experiments/cycle-2026-06-07T14-04-54Z.md`

## Main findings

- The highest-yield `sf11` move is still zero-state removal, and the strongest locally evidenced frontier remains the supply-chain/plugin route represented here by the GitHub Copilot cloud-agent trust-boundary candidate.
- The second-best route is not broad security harvesting but incident-specific scholarly attachment to already seeded independent-lab rows `LIMEN-000006` and `LIMEN-000007`.
- The best locally documented scholarly signals remain near misses only: they overlap at framework level (`LangChain`, `LlamaIndex`, `Langflow`) but mismatch the live LIMEN CVEs or vulnerable paths, so counting them would inflate authority.
- Negative result: Langflow notice-depth repair remains useful but still ranks behind both family-level zero-state routes, even though its exact AVID join makes it rhetorically tempting.

## Paper-readiness delta

LIMEN now has one lane-local, machine-readable `sf11` route selector that converts the shared security gap stack into a concrete next-move board for article, dashboard, and thesis work. This reduces the chance that future cycles spend effort on lower-yield Langflow/CrewAI polishing or overread framework-overlap scholarship as incident-specific security support.

## Remaining blocker

The security package still lacks either (a) one event-grade public supply-chain/plugin row or (b) one incident-specific scholarly attachment to a live seeded row. Until one of those arrives, the `sf11` family remains reviewer-fragile on mechanism balance and authority balance.

## Next smallest publishability move

1. If an `sf11` route opens, try one exact public advisory/incident probe for the Copilot-cloud-agent trust-boundary frontier before any new same-row notice polishing.
2. If the route is exploit-local rather than web-local, search only for incident-specific scholarly or conference support that maps directly to `LIMEN-000006` / `LIMEN-000007` or `LIMEN-000008`; preserve near-miss papers as negative evidence if the CVE or vulnerable path differs.
3. Keep Langflow vendor-notice repair explicitly fourth in order unless a direct official remediation statement becomes trivially available.
