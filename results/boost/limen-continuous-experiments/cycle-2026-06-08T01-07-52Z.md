# LIMEN continuous experiment: mental health chatbot zero-seed drift audit

Timestamp UTC: 2026-06-08T01:07:52Z
Lane: limen-continuous-experiments

Question: after the prior zero-seed control, does the newer `mental health chatbot` mining run change LIMEN's health-category evidence state, or does it only add more recursive local volume?

Inputs:
- results/cpu-mining/health-finance-education/20260607T233820Z/matches.tsv
- results/cpu-mining/health-finance-education/20260608T010011Z/matches.tsv
- results/boost/limen-continuous-experiments/mental-health-chatbot-zero-seed-control-audit-v0.1.tsv
- results/dashboard/legal-uncertainty-matrix.tsv
- results/dashboard/taxonomy-heatmap.tsv

Method:
1. Compared exact `mental health chatbot` hits across the prior bounded audit run and the newer `health-finance-education` mining run.
2. Bucketed local hits into project spine (`journal.md`, `next.md`, `manifest.json`), boost helper surfaces, and recursive `results/cpu-mining/*` surfaces.
3. Checked again whether any hit now touches `data/cases/*` or `sources/*`.
4. Verified whether the external-root profile changed.
5. Routed the result back to LIMEN's live zero-seed health/legal-uncertainty surfaces.

Findings:
- Raw query volume increased from 22 to 37 exact hits.
- The increase is entirely local (+15 local hits, +0 external hits).
- The lone external hit is unchanged: the same GAIA governance-monitor snapshot about companion/mental-health chatbot legislation.
- `data/cases/*` remains 0 hits in both runs.
- `sources/*` remains 0 hits in both runs.
- The newer run's extra local volume comes from LIMEN self-reference: +6 project-spine hits, +6 boost-helper hits, and +3 recursive cpu-mining hits.
- Result: the health-category interpretation is stable under volume drift. This query still functions as a negative-control/zero-seed methods artifact, not as a candidate-case discovery route.

Negative result made explicit:
- The apparent jump from 22 to 37 matches does not represent new health-sector public evidence. It only reflects additional LIMEN-local narration and recursive mining traces after the first negative-result audit.

Paper/thesis use:
- Methods note on why raw mining growth can be evidence-neutral under recursive local indexing.
- Dashboard limitation tooltip for `health_medical_or_mental_health`.
- Reviewer-response defense against any claim that repeated health-query volume implies improved source coverage.

Next smallest publishability move:
- Reopen this route only if one exact official safety notice, court/regulator filing, provider corrective notice, or institution-specific public incident record appears for the health category, or if a different zero-seed query adds a genuinely new limitation pattern rather than repeating this same self-reference drift.
