# LIMEN continuous experiment: keep-set self-reference drift audit

Timestamp UTC: 2026-06-08T02:15:31Z
Lane: limen-continuous-experiments

Question: after the latest overnight CPU-mining reruns, do the queries previously kept for selective exploitation still behave like bounded discovery aids, or have they drifted further into self-reference and helper-surface amplification?

Inputs:
- results/boost/limen-continuous-experiments/cpu-mining-exploitation-signal-composition-v0.1.tsv
- latest results/cpu-mining/*/matches.tsv and summary.json surfaces as of 2026-06-08T02:15:31Z

Method:
1. Reused only the 23 query rows previously carried forward in `cpu-mining-exploitation-signal-composition-v0.1.tsv`.
2. Re-opened the newest `matches.tsv` for each shard and bucketed LIMEN-local hits into candidate/source anchors, reusable downstream surfaces, boost helpers, and recursive cpu-mining artifacts.
3. Compared the newest run against the earlier exploitation-signal baseline to measure how much new local volume came from recursive traces, boost/helper spread, or anchor-bearing signal.
4. Marked queries conservatively as `locked_self_reference_zero_anchor`, `growth_without_anchor_gain`, `some_anchor_growth_but_still_thin`, or `stable_or_mixed`.

Findings:
- Across the 23 previously kept queries, local match volume rose by 2652 rows.
- Only 129 of those added local rows were anchor-bearing reusable signal, while 493 came from recursive cpu-mining traces and 2099 came from boost-helper spread.
- Drift states now split as: 2 `growth_without_anchor_gain`, 4 `locked_self_reference_zero_anchor`, 7 `some_anchor_growth_but_still_thin`, and 10 `stable_or_mixed`.
- The clearest helper-amplification failures are `security-agentic / LLM vulnerability` (+1559 local, +1537 boost, signal 0 -> 1) and `security-agentic / data exfiltration` (+353 local, +330 boost, signal 0 -> 1).
- Four queries are now explicit zero-anchor self-reference locks: health-finance-education / AI insurance, health-finance-education / workplace surveillance, residual-weird / AI misuse, residual-weird / AI scandal.
- The strongest still-thin but reusable frontiers are `legal-research / procedural contamination` (15 signal hits; 8.47% signal share), `identity-provenance / synthetic identity` (9 signal hits; 5.81%), `security-agentic / AI vulnerability` (23 signal hits; 3.16%), and `security-agentic / tool misuse` (11 signal hits; 1.98%).

Negative result made explicit:
- Overnight growth in the keep set is mostly not discovery growth. The dominant change is internal recirculation through boost notes and recursive mining traces, so raw rerun expansion should not be narrated as broader LIMEN evidence coverage.

Paper/thesis use:
- Methods/control table on how repeated mining reruns can inflate local counts without materially improving evidence support.
- Dashboard warning badge for mining queries whose local growth is dominated by helper spread rather than new anchor-bearing evidence.
- Reviewer-response support for freezing overstimulated query families while preserving a small bounded subset for methods or appendix reuse.

Next smallest publishability move:
- Reopen a kept query only if a future run adds a new `data/cases`, `sources`, or clearly external authority anchor; otherwise use the query only for methods/dashboard saturation and do not count local volume drift as corpus growth.
