# LIMEN continuous experiment: CPU-mining reopen gate board

Timestamp UTC: 2026-06-08T02:52:11Z
Lane: limen-continuous-experiments

Question: after the keep-set drift audit, which surviving CPU-mining queries should remain frozen as methods-only negative results, which should reopen only on a new external anchor, and which four named frontiers deserve bounded monitoring rather than raw rerun repetition?

Inputs:
- results/boost/limen-continuous-experiments/cpu-mining-keep-set-self-reference-drift-v0.1.tsv
- results/boost/limen-continuous-experiments/cpu-mining-exploitation-signal-composition-v0.1.tsv
- results/boost/limen-continuous-experiments/cpu-mining-query-contamination-retune-matrix-v0.1.tsv
- next.md lines 8-11 (current lane route note)

Method:
1. Reused the 23 previously kept query rows from the drift audit as the canonical denominator.
2. Joined each row to the earlier exploitation-signal and contamination/retune boards.
3. Converted the lane's current prose rule into three explicit gate states: `freeze_methods_only`, `reopen_only_on_new_external_anchor`, and `frontier_monitor_only`.
4. Reserved `frontier_monitor_only` only for the four still-thin frontiers named explicitly in the current route note: `procedural contamination`, `synthetic identity`, `AI vulnerability`, and `tool misuse`.
5. Kept the negative-result ceiling explicit: no gate state is a case promotion, denominator change, or claim upgrade by itself.

Findings:
- The 23-query keep set now splits into 4 `frontier_monitor_only`, 13 `reopen_only_on_new_external_anchor`, and 6 `freeze_methods_only` rows.
- The bounded monitoring frontier is now explicit rather than implied: identity-provenance / synthetic identity, legal-research / procedural contamination, security-agentic / AI vulnerability, security-agentic / tool misuse.
- The strongest methods-only freeze rows remain the four zero-anchor self-reference locks plus the two growth-without-anchor-gain failures: health-finance-education / AI insurance, health-finance-education / workplace surveillance, residual-weird / AI misuse, residual-weird / AI scandal, security-agentic / LLM vulnerability, security-agentic / data exfiltration.
- This board makes the lane's reopening rule machine-readable for dashboard badges, methods notes, and reviewer-response reuse: unchanged reruns are not progress unless a query gains a genuinely new external authority/source/data-cases anchor.

Negative result made explicit:
- Most of the surviving keep set should still not be reopened for corpus-growth narration. The board converts that negative result into per-query gate states so helper-spread and recursive-growth rows remain count-silent by default.

Paper/thesis use:
- Methods table on when repeated mining reruns are frozen, conditionally reopenable, or frontier-monitored.
- Dashboard legend/badge layer for mining queries whose apparent growth is not publishable evidence growth.
- Reviewer-response support for why future mining cycles should target external-anchor change rather than another unchanged rerun.

Next smallest publishability move:
- Reopen continuous-experiments only if one of the four named frontier rows gains a new external anchor or materially stronger anchor-bearing share, or if any currently frozen/held row acquires a genuinely new `data/cases`, `sources`, or external-authority anchor.
