# Continuous experiments cycle note — 2026-06-08T07:40:44+02:00

Artifact created:
- `results/boost/limen-continuous-experiments/frontier-residue-public-anchor-lift-audit-v0.1.tsv`

Why this cycle:
- The lane's current front-door bundle already proved that monitored frontier growth remained thin and mostly local.
- The remaining ambiguity was whether any of the seven traced non-LIMEN residue hits could be responsibly lifted into separately citable public anchors without reopening broad crawling.

Bounded experiment:
- Audited every row in `results/boost/limen-continuous-experiments/frontier-external-anchor-trace-board-v0.1.tsv`.
- For each residue hit, checked whether the cited context itself exposed a public URL.
- When the residue was an HTML snapshot, allowed one canonical-URL fallback.
- For extracted public URLs, ran a small live reachability/title check only; no bulk crawling.

Result summary:
- `synthetic identity`: all `3/3` traced non-LIMEN residues remain local method/manuscript context only; no separately citable public URL was exposed in bounded context.
- `AI vulnerability`: the only liftable public URLs in traced residue were CrowdStrike service/program pages plus one ServiceNow product-page canonical URL from a local snapshot.
- CrowdStrike URLs were live at audit time (`200`) but resolved to generic service/program surfaces, not row-level authority, procurement, or case-specific incident anchors.
- ServiceNow's canonical product URL was recoverable from the local snapshot, but the bounded live fetch timed out and the canonical path still indicates a generic product surface rather than a case-specific anchor.

Paper-readiness delta:
- LIMEN now has one machine-readable residue-to-public-anchor audit showing that the currently traced frontier remainder still does not justify reopening `synthetic identity` or `AI vulnerability` as if public case/authority evidence had widened.
- This makes negative-result language more reviewer-checkable: some public pages are extractable from residue, but they are generic program/service/product surfaces, not threshold-changing anchors.

Claims kept conservative:
- No case was added, promoted, merged, or reclassified.
- No frontier share or evidence tier changed.
- Live checks were limited to a handful of extracted URLs and do not prove the non-existence of stronger public anchors elsewhere.
- Generic vendor/program pages were not allowed to count as row-level public evidence growth.

Remaining blocker:
- The next real gain still requires one genuinely new public authority, procurement, or case-specific source/data-case anchor in a monitored row; this cycle only audited why the current traced residue still falls below that threshold.
