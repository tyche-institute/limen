# Continuous experiments cycle note — 2026-06-08T08:16:02+02:00

Artifact created:
- `results/boost/limen-continuous-experiments/frontier-latest-run-currentness-audit-v0.1.tsv`

Why this cycle:
- The lane's front-door residue bundle was still anchored to the 20260608T0215xx monitored runs.
- Newer 20260608T0600xx monitored runs already existed on disk, so the smallest useful experiment was a currentness audit: test whether the newer volume actually widened the non-LIMEN/public-anchor surface or only added more local LIMEN hits.

Bounded experiment:
- Compared the previously audited monitored runs (`20260608T021547Z`, `20260608T021523Z`, `20260608T021528Z`) against the latest available monitored runs (`20260608T060104Z`, `20260608T060059Z`, `20260608T060100Z`).
- Recomputed total match counts and non-LIMEN root profiles for the four monitored frontier queries only.
- Did not reopen broad crawling or count generic vendor/program pages as new row-level public evidence.

Result summary:
- `synthetic identity` rose from `158` to `205` total matches, but non-LIMEN residue stayed fixed at `3`, all still GAIA-local.
- `procedural contamination` rose from `177` to `216` total matches, but non-LIMEN residue stayed at `0`.
- `AI vulnerability` rose from `732` to `787` total matches, but non-LIMEN residue stayed fixed at `4` (`3` GAIA-local + `1` Atlas-local), matching the already-audited generic CrowdStrike/ServiceNow ceiling.
- `tool misuse` rose from `556` to `582` total matches, but non-LIMEN residue stayed at `0`.

Paper-readiness delta:
- LIMEN now has one machine-readable latest-run currentness audit showing that the post-0215 monitored growth is still local-volume drift rather than public-anchor widening.
- This keeps reviewer-safe frontier language current without overstating the meaning of larger raw match totals.

Claims kept conservative:
- No case was added, promoted, merged, or reclassified.
- No frontier was reopened.
- No generic vendor/program/product page was counted as a new authority, procurement, or case-specific source anchor.
- The audit updates run currentness only; it does not change the existing `3 / 0 / 4 / 0` non-LIMEN frontier profile.

Remaining blocker:
- The next real gain still requires one genuinely new public authority, procurement, or case-specific source/data-case anchor in a monitored row; this cycle only confirms that the newer monitored runs still do not clear that threshold.
