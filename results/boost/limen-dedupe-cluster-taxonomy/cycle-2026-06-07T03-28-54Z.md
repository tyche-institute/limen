# limen-dedupe-cluster-taxonomy cycle note

- Timestamp (UTC): 2026-06-07T03:28:54Z
- Lane: `limen-dedupe-cluster-taxonomy`
- Project: `limen-ai-edge-case-atlas`
- Sprint / goal-card context: hostile-reviewer pass; duplicate-cluster and residual-category artifacts must be reviewer-legible and count-safe.

## What I did

- Re-read stop controls, sprint/goal-card priorities, the duplicate-cluster ledger, the taxonomy delta, and the lane-011 normalized-join ledger.
- Verified from local case files that the live package still contains 23 visible rows and 18 unique lineages after collapsing the five lane-005 workflow duplicates.
- Synchronized `results/boost/limen-boost-011/normalized-join-key-ledger.tsv` with the live duplicate state by:
  - adding the authoritative Nate row;
  - treating `data/cases/security-agentic-candidates.jsonl:LIMEN-000005` as an identifier-collision blocker as well as a same-family non-merge context;
  - upgrading the base Finnish Yle row from `title_gloss_only` to `machine_read_direct_source_extraction_note`.
- Refreshed `results/clusters/status.md` and `results/taxonomy/taxonomy-delta-v0.1.md`, and re-anchored the two live duplicate rows in `results/clusters/duplicate-clusters-v0.1.tsv` with a current-cycle timestamp.

## Paper-readiness delta

The duplicate/taxonomy package is now internally more coherent for hostile-reviewer use: duplicate control and normalized joins agree on the Nate collision and the Yle evidence-state upgrade, so downstream counting logic has one fewer avoidable inconsistency. The package still records zero confirmed cross-source event-level duplicate collapses.

## Claims kept conservative

- No new event-level merge was introduced.
- No new top-level taxonomy label was promoted.
- The Nate row remains charging-stage bounded.
- The Yle row remains direct-source-resolved broadcaster evidence, not official corroboration.

## Remaining blocker

The next blocker is downstream export coherence rather than dedupe discovery: some lane-011 aggregate-safe exports still reflect a pre-sync 17-lineage state, and the project still lacks a globally unique case namespace.

## Suggested next route

Refresh the lane-011 aggregate-safe exports and any downstream manuscript/dashboard count prose so they inherit the synchronized 23-row / 18-lineage normalized package before stronger figure or comparative language is used.
