# limen-dedupe-cluster-taxonomy cycle note

- Timestamp (UTC): 2026-06-07T04:47:17Z
- Lane: `limen-dedupe-cluster-taxonomy`
- Project: `limen-ai-edge-case-atlas`
- Sprint / goal-card context: hostile-reviewer pass; duplicate-cluster and residual-category artifacts must be reviewer-legible and count-safe.

## What I did

- Re-read stop controls, sprint / goal-card priorities, the duplicate-cluster ledger, the taxonomy delta, and the current local case inventories after confirming there was still no active `/srv/tyche/STOP` or project `STOP` file.
- Verified from local case files that the live package has advanced to 26 visible rows, driven by authoritative promotion of two SEC settlement rows that still remained in shard 009.
- Added two stable duplicate clusters to `results/clusters/duplicate-clusters-v0.1.tsv` so Delphia and Global Predictions now collapse across authoritative + shard-local inventories without being misread as new cases.
- Refreshed `results/clusters/status.md` and `results/taxonomy/taxonomy-delta-v0.1.md` so the duplicate / taxonomy package now reflects the live 26-row / 19-lineage base rather than the stale 24-row / 19-lineage state.

## Paper-readiness delta

LIMEN's duplicate/taxonomy package now blocks double counting in the finance / AI-washing slice after shared-project promotion of the SEC rows. The package also keeps the security subtype signal and residual queue logic current without inflating event counts.

## Claims kept conservative

- No new independent-source event-level merge was introduced.
- No new top-level taxonomy label was promoted.
- The two SEC additions are same-event promotion duplicates, not new enforcement matters.
- Multilingual low-ceiling rows and the Langflow row remain confidence-capped.

## Remaining blocker

The next blocker is downstream coherence rather than dedupe discovery: `results/boost/limen-boost-011/normalized-join-key-ledger.tsv` still reflects a 23-row state and does not yet inherit either the six-row security inventory or the new SEC promotion duplicate controls.

## Suggested next route

Refresh the lane-011 normalized ledger and any aggregate-safe exports, then re-run dashboard / manuscript count checks so publication-facing finance and AI-washing prose inherits the live 26-row / 19-lineage package.
