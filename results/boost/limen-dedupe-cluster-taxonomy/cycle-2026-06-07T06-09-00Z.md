# limen-dedupe-cluster-taxonomy cycle note

- Timestamp (UTC): 2026-06-07T06:09:00Z
- Lane: `limen-dedupe-cluster-taxonomy`
- Project: `limen-ai-edge-case-atlas`
- Sprint / goal-card context: hostile-reviewer pass; keep duplicate-control and taxonomy artifacts reviewer-legible, count-safe, and machine-consumable.

## What I did

- Re-read stop controls, the hostile-reviewer sprint, the LIMEN goal card, the live duplicate-cluster ledger, the taxonomy delta, and the seeded case inventories after confirming there was still no active `/srv/tyche/STOP` or project `STOP` file.
- Recomputed the live local base from local JSONL inventories and re-verified that the package still contains 27 visible rows and 20 unique lineages after collapsing seven stable duplicate clusters.
- Verified that the duplicate-review posture is unchanged in substance: 7 `stable_duplicate_cluster`, 5 `reviewed_not_duplicate`, 5 `identifier_collision_blocker`, and 0 confirmed independent-source event-level merges.
- Normalized `results/clusters/duplicate-clusters-v0.1.tsv` so lane-005 and shard-009 rows now use full project-relative `member_case_ids` paths instead of mixed `boost-005/...` and `boost-009/...` shorthand.
- Refreshed `results/clusters/status.md` and `results/taxonomy/taxonomy-delta-v0.1.md` so they document the machine-readability hardening and preserve the current 27-row / 20-lineage denominator.

## Paper-readiness delta

The duplicate/taxonomy package is now easier to consume safely in downstream scripts, figures, and manuscript tables because stable duplicate members no longer require ad hoc path-alias repair before collapse. This is a small but real hostile-reviewer improvement: the count-safe denominator is unchanged, but the evidence path to reproduce it is cleaner.

## Claims kept conservative

- No new case was added.
- No new event-level merge was introduced.
- No new top-level taxonomy label was promoted.
- Security-subtype and multilingual-confidence cautions remain unchanged.

## Remaining blocker

`results/boost/limen-boost-011/normalized-join-key-ledger.tsv` still reflects a 23-row state and remains behind the live package, so shared normalized exports are still the main publication-facing coherence risk.

## Suggested next route

Refresh the lane-011 normalized ledger and any aggregate-safe exports so dashboard/manuscript count logic inherits both the SEC promotion duplicate controls and the current seventh security row without local alias-repair workarounds.
