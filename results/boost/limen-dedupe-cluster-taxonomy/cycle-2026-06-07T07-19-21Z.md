# limen-dedupe-cluster-taxonomy cycle note

- Timestamp (UTC): 2026-06-07T07:19:21Z
- Lane: `limen-dedupe-cluster-taxonomy`
- Project: `limen-ai-edge-case-atlas`
- Sprint / goal-card context: hostile-reviewer pass; keep duplicate-control and taxonomy artifacts reviewer-legible, count-safe, and machine-consumable.

## What I did

- Re-read stop controls, the hostile-reviewer sprint, the LIMEN goal card, the live duplicate-cluster ledger, the taxonomy delta, the seeded case inventories, and the refreshed normalized join ledger after confirming there was still no active `/srv/tyche/STOP` or project `STOP` file.
- Re-verified that the live local base is still 28 visible rows and 21 unique lineages after collapsing seven stable duplicate clusters.
- Added `REVIEW-NOMERGE-FILEREAD-001` to `results/clusters/duplicate-clusters-v0.1.tsv` so Claude Code unauthorized file read and CrewAI arbitrary local file read are explicitly reviewed as distinct events sharing one repeated boundary-bypass subtype.
- Refreshed `results/clusters/status.md` and `results/taxonomy/taxonomy-delta-v0.1.md` so they expose the new file-disclosure subtype cue and keep the duplicate/taxonomy package aligned with the live denominator.

## Paper-readiness delta

LIMEN's duplicate/taxonomy package now shows a second high-confidence repeated security subtype beyond execution surfaces: unauthorized local file disclosure / boundary bypass across distinct agent frameworks. That makes the security/agentic figure and methods layers more interpretable while preserving conservative event counts.

## Claims kept conservative

- No new case was added by this lane.
- No new stable same-event merge was introduced.
- No new top-level taxonomy label was promoted.
- The Claude Code and CrewAI rows are treated as separate vulnerability lineages, not as prevalence evidence or a same-incident merge.

## Remaining blocker

The main blocker remains the lack of one join-safe cross-surface event package spanning authority + media and/or comparator-database representations of the same event.

## Suggested next route

Seed one cross-surface event package with file-qualified stable IDs so LIMEN can test true duplicate resolution across evidence families instead of only reviewing intra-project collisions and subtype non-merges.
