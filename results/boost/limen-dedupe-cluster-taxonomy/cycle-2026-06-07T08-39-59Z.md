# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-07T08:39:59Z
Lane id: `limen-dedupe-cluster-taxonomy`
Project root: `/srv/tyche/projects/limen-ai-edge-case-atlas`

## Why this route

After re-reading the sprint directive and goal card, the highest-value paper-readiness move was denominator and join-safety repair, not new clustering. The live security package had gained `LIMEN-000009`, but the dedupe/taxonomy control surfaces still reflected the older pre-ingest denominator and the normalized join ledger did not yet carry the new row.

## Artifacts touched

- `results/boost/limen-boost-011/normalized-join-key-ledger.tsv`
- `results/clusters/status.md`
- `results/taxonomy/taxonomy-delta-v0.1.md`
- `journal.md`

## What changed

- Verified the current package now sits at 29 visible rows / 24 unique lineages.
- Confirmed no new same-event duplicate cluster and no new taxonomy label were justified.
- Patched the normalized join ledger so `data/cases/security-agentic-candidates.jsonl:LIMEN-000009` now appears with `no_cluster_review_row` rather than being absent from the export layer.
- Refreshed the cluster/taxonomy status surfaces so they no longer quote the older `28 / 23` denominator or the older `8 / 8` security counts.

## Paper-readiness delta

The duplicate/taxonomy package now acknowledges the newest security lineage without pretending it already belongs in a reviewed overlap cluster. That reduces reviewer-visible denominator drift and prevents join-safe downstream tables from silently excluding the PraisonAI row.

## Remaining blocker

The package still lacks one join-safe cross-surface event package spanning at least two evidence families.

## Next smallest publishability move

Seed one overlap package where a single event appears across authority + media and/or comparator-database surfaces with file-qualified stable identifiers, then rerun duplicate review on that package.
