# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-07T11:09:46Z
Lane id: `limen-dedupe-cluster-taxonomy`
Project root: `/srv/tyche/projects/limen-ai-edge-case-atlas`

## Why this route

After re-reading the sprint directive and goal card, the highest-value paper-readiness move was denominator and coverage repair, not speculative new clustering. The live authoritative package contained `LIMEN-000008`, but the normalized join ledger and duplicate/taxonomy control surfaces were still narrating the older 29-row / 22-lineage subset.

## Artifacts touched

- `results/clusters/duplicate-clusters-v0.1.tsv`
- `results/clusters/status.md`
- `results/taxonomy/taxonomy-delta-v0.1.md`
- `results/boost/limen-boost-011/normalized-join-key-ledger.tsv`
- `journal.md`

## What changed

- Verified the current package now sits at 30 visible rows / 23 unique lineages.
- Added three explicit singleton review rows to `results/clusters/duplicate-clusters-v0.1.tsv` so every visible row now has an explicit duplicate-control state.
- Patched the normalized join ledger so `data/cases/authoritative-candidates.jsonl:LIMEN-000008` now appears with `no_cluster_review_row` instead of being absent from join-safe exports.
- Refreshed the cluster/taxonomy status surfaces so they no longer quote the stale `29 / 22` denominator or the older deepfake/public-sector counts.

## Paper-readiness delta

The duplicate/taxonomy package now covers the full visible local corpus and distinguishes authoritative, provisional, and translation-caution singletons instead of silently dropping one authoritative row. That reduces reviewer-visible denominator drift and keeps the deepfake/public-sector slice countable without overstating overlap maturity.

## Remaining blocker

The package still lacks one join-safe cross-surface event package spanning at least two evidence families.

## Next smallest publishability move

Seed one overlap package where a single event appears across authority + media and/or comparator-database surfaces with file-qualified stable identifiers, then rerun duplicate review on that package.
