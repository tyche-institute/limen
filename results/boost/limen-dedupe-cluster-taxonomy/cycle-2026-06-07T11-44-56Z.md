# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-07T11:44:56Z
Lane id: `limen-dedupe-cluster-taxonomy`
Project root: `/srv/tyche/projects/limen-ai-edge-case-atlas`

## Why this route

After re-reading the sprint directive and goal card, the highest-value paper-readiness move was control-surface repair, not speculative new clustering. The live core package had already promoted the Slovenian police ORION row into `data/cases/authoritative-candidates.jsonl`, but the duplicate/taxonomy controls still treated only the earlier multilingual and queue versions as reviewed.

## Artifacts touched

- `results/clusters/duplicate-clusters-v0.1.tsv`
- `results/clusters/status.md`
- `results/taxonomy/taxonomy-delta-v0.1.md`
- `journal.md`

## What changed

- Verified the current scoped package now sits at 31 visible rows / 23 unique lineages.
- Refreshed the existing ORION stable duplicate cluster so the authoritative follow-through row, multilingual base row, and lane-005 queue copy count as one lineage.
- Added two new identifier-collision blockers for `LIMEN-000008` and `LIMEN-000009` because newer authoritative rows now reuse bare IDs already occupied by unrelated security records.
- Refreshed the cluster/taxonomy status surfaces so they no longer quote the stale `30 / 23` control denominator.

## Paper-readiness delta

The duplicate/taxonomy package now carries the promoted ORION row through explicit duplicate control, preserves its multilingual provenance trail, and exposes the two newer bare-ID collision hazards before they can distort dashboard or manuscript counts.

## Remaining blocker

The package still lacks one join-safe same-event overlap package spanning at least two evidence families with stable file-qualified identifiers.

## Next smallest publishability move

Build one authority+media and/or authority+comparator overlap package around a single clearly shared event, then rerun duplicate review on that package.
