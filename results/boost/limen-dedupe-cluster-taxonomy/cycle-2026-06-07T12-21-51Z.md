# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-07T12:21:51Z
Lane id: `limen-dedupe-cluster-taxonomy`

## Why this cycle

The live duplicate-control TSV had already absorbed ORION plus the newer FCC/PraisonAI collision blockers, but the reviewer-facing status and taxonomy notes still understated how many visible rows now carry overlapping control semantics.

## What changed

1. Refreshed `results/clusters/duplicate-clusters-v0.1.tsv` so the ORION, FCC, and PraisonAI rows now state their simultaneous multi-state control burden directly inside the review notes.
2. Refreshed `results/clusters/status.md` to correct reviewed-non-merge row coverage (`11`, not `8`) and to surface that `16/31` visible rows now carry two or more duplicate-control states.
3. Refreshed `results/taxonomy/taxonomy-delta-v0.1.md` so the taxonomy note no longer reads the package as if duplicate control were single-state.

## Current control summary

- visible rows in scope: `31`
- stable-or-standalone lineages after collapse: `23`
- `stable_duplicate_cluster` packages: `7` governing `15` rows
- `reviewed_not_duplicate` packages: `7` governing `11` rows
- `identifier_collision_blocker` packages: `9` governing `20` rows
- `no_cluster_review_row` packages: `3` governing `3` rows
- visible rows with 2+ control states: `16`

## Publication/thesis use

This strengthens duplicate-review figure captions, taxonomy heatmap denominators, and methods prose by making it explicit that ORION, FCC, and PraisonAI cannot be represented safely with one control badge each.

## Remaining blocker

Downstream shared exports still mostly flatten duplicate control to one field, so the next high-value move remains a compact propagation contract or export sidecar that carries multi-state semantics into shared dashboard/manuscript surfaces.
