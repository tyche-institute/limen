# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-07T12:56:56Z
Lane id: `limen-dedupe-cluster-taxonomy`

## Why this cycle

The duplicate/taxonomy package already had the right substantive denominator (`31 / 23`), but its control semantics were still too prose-dependent for dashboard/table reuse. The highest-value small move was to make collapse, collision, and multistate burden travel directly inside the machine-readable duplicate ledger.

## What changed

1. Refreshed `results/clusters/duplicate-clusters-v0.1.tsv` with four new export fields: `lineage_counting_rule`, `namespace_collision_flag`, `overlap_control_states`, and `supports_multi_state_control`.
2. Refreshed `results/clusters/status.md` to explain the ledger-level export upgrade and keep the `31 visible / 23 lineage` contract explicit.
3. Refreshed `results/taxonomy/taxonomy-delta-v0.1.md` so taxonomy-safe counting is described as a machine-readable method layer, not only a prose warning.

## Current control summary

- visible rows in scope: `31`
- stable-or-standalone lineages after collapse: `23`
- `stable_duplicate_cluster` packages: `7` governing `15` rows
- `reviewed_not_duplicate` packages: `7` governing `11` rows
- `identifier_collision_blocker` packages: `9` governing `20` rows
- `no_cluster_review_row` packages: `3` governing `3` rows
- visible rows with 2+ control states: `16`

## Publication/thesis use

This strengthens duplicate-review figure captions, taxonomy heatmap denominators, and future Figure 7 multistate export work by keeping ORION, FCC, and PraisonAI machine-readable as rows that cannot safely be flattened to one badge.

## Remaining blocker

The next substantive dedupe gain still requires one join-safe same-event overlap package across two evidence families or a downstream multistate-safe export sidecar that proves the new ledger fields are being consumed correctly.
