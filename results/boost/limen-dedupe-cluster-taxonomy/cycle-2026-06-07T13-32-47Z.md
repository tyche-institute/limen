# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-07T13:32:47Z
Lane id: `limen-dedupe-cluster-taxonomy`

## Why this cycle

The duplicate/taxonomy package had become stale against the live authoritative inventory. Two new FTC non-finance AI-claim rows existed in `data/cases/authoritative-candidates.jsonl`, but the duplicate ledger still reflected the earlier `31 / 23` denominator and left those rows outside explicit duplicate-review control.

## What changed

1. Appended two explicit singleton review rows to `results/clusters/duplicate-clusters-v0.1.tsv` for `LIMEN-000010` (DoNotPay) and `LIMEN-000011` (Workado).
2. Refreshed `results/clusters/status.md` to move the controlled duplicate denominator from `31 / 23` to `33 / 25`.
3. Refreshed `results/taxonomy/taxonomy-delta-v0.1.md` so the taxonomy package reflects the widened non-finance AI-claim coverage without inventing a new top-level label.

## Current control summary

- visible rows in scope: `33`
- stable-or-standalone lineages after collapse: `25`
- `stable_duplicate_cluster` packages: `7` governing `15` rows
- `reviewed_not_duplicate` packages: `7` governing `11` rows
- `identifier_collision_blocker` packages: `9` governing `20` rows
- `no_cluster_review_row` packages: `5` governing `5` rows
- visible governed rows with 2+ control states: `16`

## Publication/thesis use

This keeps taxonomy heatmaps, duplicate graphs, and reviewer appendices synchronized with the live authoritative inventory. It also makes the new FTC rows reusable as bounded non-finance AI-claim exemplars without overstating them as duplicates, recurrence packages, or new taxonomy labels.

## Remaining blocker

The next substantive dedupe gain still requires one join-safe same-event overlap package across two evidence families or one explicit downstream validator proving that multistate duplicate-control fields are being consumed correctly in figure/table exports.
