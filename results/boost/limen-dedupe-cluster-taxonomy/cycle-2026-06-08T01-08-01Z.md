# limen-dedupe-cluster-taxonomy cycle note

Cycle timestamp (UTC): 2026-06-08T01:08:01Z
Lane id: `limen-dedupe-cluster-taxonomy`

## Why this cycle

The strongest near-publication move was no longer another merge guess or taxonomy expansion. The duplicate/taxonomy bundle already carried the right substantive decisions, but the lane needed a fresh overnight revalidation proving that the governed denominator, held-row boundaries, and required outputs still matched the live case files after the latest saturation wave.

## Artifacts created

- `results/boost/limen-dedupe-cluster-taxonomy/duplicate-taxonomy-revalidation-v0.1.tsv`
- `results/boost/limen-dedupe-cluster-taxonomy/cycle-2026-06-08T01-08-01Z.md`

## Artifacts refreshed

- `results/clusters/status.md`
- `results/taxonomy/taxonomy-delta-v0.1.md`
- `results/boost/limen-dedupe-cluster-taxonomy/status.md`

## Main finding

The duplicate/taxonomy package remains stable and reviewer-safe under the live case inventory:

- `results/clusters/duplicate-clusters-v0.1.tsv` remains unchanged at `35` review rows.
- The count-bearing seeded package remains `34 review rows / 37 governed records / 27 lineages`.
- The extended reviewed local surface remains `35 / 38 / 28` because the residual frontier singleton stays ledger-visible but count-silent.
- Live `data/cases` inventory is now `30` rows total (`14` authoritative, `10` security, `6` multilingual), with exactly two intentional governance gaps: `LIMEN-000013` and `LIMEN-000014`.

## Interpretation

This was a currentness-and-contract cycle, not a new empirical clustering cycle. The gain is that publication-facing dedupe/taxonomy surfaces now explicitly say the lane was revalidated against the live overnight inventory and still holds the same conservative denominator contract.

## Observatory hook

The revalidation TSV can feed a denominator-control or QA panel with fields for `check_id`, `subject`, `expected`, `observed`, `status`, and `note`, plus a small held-row widget showing that the only intentional live-governance gaps remain Rytr and Air AI.
