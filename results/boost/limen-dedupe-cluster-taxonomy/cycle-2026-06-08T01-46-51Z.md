# limen-dedupe-cluster-taxonomy cycle note

Cycle timestamp (UTC): 2026-06-08T01:46:51Z
Lane id: `limen-dedupe-cluster-taxonomy`

## Why this cycle

The strongest next paper-readiness move was no longer another denominator recheck. The duplicate and taxonomy package was already stable, but the reviewed-not-duplicate rows and singleton holds still required manual reading to recover which repeated patterns were methods-note ready and which rows still had to remain residual or watch-state only.

## Artifact created

- `results/boost/limen-dedupe-cluster-taxonomy/taxonomy-subtype-pressure-board-v0.1.tsv`

## Artifacts refreshed

- `results/clusters/status.md`
- `results/taxonomy/taxonomy-delta-v0.1.md`
- `results/boost/limen-dedupe-cluster-taxonomy/status.md`

## Main finding

The duplicate/taxonomy package remains stable at `34 / 37 / 27` for the count-bearing seeded scope and `35 / 38 / 28` for the extended reviewed local surface, but LIMEN now has a clearer internal taxonomy-pressure map:

- `model_mediated_execution_surface_failures` is the strongest subtype-ready methods-note candidate from reviewed non-merge rows.
- `agent_file_disclosure_and_tool_boundary_failures` also has multi-row support and is usable as a bounded security/agentic subtype overlay.
- `official_non_finance_false_ai_capability_enforcement`, `regulated_finance_ai_washing_enforcement`, and `biometric_governance_and_facial_recognition_oversight` remain reviewer-safe recurrence families, not duplicate merges.
- `framework_runtime_state_and_memory_trust_boundary_failures` remains below promotion threshold because the support still sits inside one framework family.
- The fake-article citation-network row remains an explicit residual singleton requiring stronger platform, publisher, indexing-service, or institutional action before any promotion.

## Interpretation

This was a structure-and-interpretability cycle, not a new merge cycle. The gain is that later dashboard, manuscript, reviewer-response, and thesis work can now reuse one machine-readable board that says which repeated patterns are subtype pressure and which rows are still singleton or residual queues, without widening the canonical top-level taxonomy or pretending that repeated mechanism alone proves prevalence.

## Observatory hook

The new TSV can feed a subtype-pressure heatmap or residual queue panel with fields for `candidate_subtype`, `source_review_rows`, `review_row_count`, `unique_lineage_count`, `dominant_categories`, `adoption_signal`, `observatory_hook`, and `caution`.
