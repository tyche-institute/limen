# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-08T03:51:54Z
Lane id: `limen-dedupe-cluster-taxonomy`
Project root: `/srv/tyche/projects/limen-ai-edge-case-atlas`

## Why this route

After the earlier live-governance audit, the next highest-value gap was no longer denominator coverage but structural legibility. The duplicate ledger already existed, yet another reader still had to reconstruct by hand whether current pressure came from same-event duplication, workflow-derived copies, bounded subtype overlays, or residual/singleton queues. Under the hostile-reviewer sprint, that ambiguity was a paper-readiness problem.

## Artifacts created or refreshed

Created:

- `results/boost/limen-dedupe-cluster-taxonomy/duplicate-taxonomy-pressure-board-v0.1.tsv`
- `results/boost/limen-dedupe-cluster-taxonomy/cycle-2026-06-08T03-51-54Z.md`

Refreshed:

- `results/clusters/status.md`
- `results/taxonomy/taxonomy-delta-v0.1.md`
- `results/boost/limen-dedupe-cluster-taxonomy/status.md`
- `results/boost/limen-dedupe-cluster-taxonomy/journal.md`
- `journal.md`

## What changed

### 1. Duplicate pressure is now decomposed machine-readably

The new TSV splits the current ledger into four reusable views:

- cluster-status counts;
- stable-duplicate family composition by source-path combination;
- reviewed-not-duplicate subtype-pressure buckets;
- singleton-pressure buckets plus multi-state join-safety burden.

### 2. Stable duplicates are mostly workflow derivatives

The canonical `9` stable duplicate rows are not dominated by undiscovered same-event overlap.

Instead they are mostly:

- `4` multilingual base-row plus queue-copy collapses;
- `2` authoritative promotion collapses from shard-009;
- `1` multilingual-to-authoritative ORION promotion chain;
- `1` FCC lifecycle absorption bridge;
- `1` Finland procedural-contamination bridge copy.

### 3. Reviewed-not-duplicate pressure is concentrated in a few recurrence families

The `8` reviewer-safe non-merge rows mainly support:

- execution-surface recurrence (`3` rows);
- boundary/identity recurrence (`3` rows);
- official false-capability recurrence (`2` rows);
- biometric-governance recurrence (`1` row).

This supports stronger explanation of why the lane did not add a new duplicate cluster or new top-level category.

### 4. Singleton and residual pressure remain explicit

The queue is concentrated in a few intelligible fronts:

- deepfake/synthetic identity (`2` singleton rows);
- official false-capability / detector-governance (`2` singleton rows);
- MCP/local tool-boundary (`1` row);
- public-sector governance anchor (`1` row);
- identity/authorization abuse (`1` row);
- research-integrity residual frontier (`1` row).

## Paper-readiness delta

- LIMEN now has one machine-readable board showing where duplicate, recurrence, and residual pressure actually sits.
- Reviewer-response and methods prose can now justify a stable denominator without sounding inert or stale.
- Dashboard/table reuse is easier because workflow derivatives, reviewed non-merges, and singleton queues are now visibly separated.

## Claims verified, rewritten, or dropped

Verified:

- the canonical ledger still remains `35` review rows with `34 / 37 / 27` core and `35 / 38 / 28` extended scope;
- the live seed inventory remains covered except for the two deliberate authoritative holds `LIMEN-000013` and `LIMEN-000014`;
- no current pressure pattern requires a new top-level taxonomy label.

Rewritten:

- duplicate and taxonomy status notes now describe structural concentration rather than only count stability.

Dropped or explicitly not made:

- no new same-event merge claim;
- no new taxonomy label claim;
- no claim that Rytr or Air AI have entered the duplicate-governed core.

## Remaining blocker

The next real gain is still one of:

1. a join-safe same-event package across at least two evidence families;
2. an explicit sidecar-admission decision for Rytr or Air AI on the extended reviewed local surface;
3. a second reviewed lineage that lifts the MCP / authorization-abuse / research-integrity singleton frontier beyond watcher status;
4. a direct-authority upgrade for `REVIEW-SINGLETON-LIMEN-008-LEAD-007`.

## Suggested next route

Hold the current duplicate ledger unless one of the blocker conditions above changes. If a new cycle is needed before that, the smallest publishability move is probably language-scope normalization inside the canonical ledger or one bounded second-lineage hunt for the MCP / authorization-abuse / research-integrity singleton fronts.