# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-08T05:02:09Z
Lane id: `limen-dedupe-cluster-taxonomy`
Project root: `/srv/tyche/projects/limen-ai-edge-case-atlas`

## Why this route

The prior duplicate/taxonomy cycle already showed that mixed `English` / `en` values were mostly export formatting drift rather than unresolved evidence ambiguity. Under the hostile-reviewer sprint, the next smallest publishability move was to internalize that rule into the canonical duplicate ledger so later dashboard, reviewer-response, and thesis consumers do not split one language surface into two buckets.

## Artifacts created or refreshed

Created:

- `results/boost/limen-dedupe-cluster-taxonomy/duplicate-language-normalization-resolution-v0.1.tsv`
- `results/boost/limen-dedupe-cluster-taxonomy/cycle-2026-06-08T05-02-09Z.md`

Refreshed:

- `results/clusters/duplicate-clusters-v0.1.tsv`
- `results/clusters/status.md`
- `results/taxonomy/taxonomy-delta-v0.1.md`
- `results/boost/limen-dedupe-cluster-taxonomy/status.md`
- `manifest.json`

## What changed

### 1. Canonical duplicate-ledger language strings are now normalized

`16` duplicate-ledger rows changed in `language_scope`, but only as export-format normalization.

Examples:

- `English` -> `en`
- `English;en` -> `en`
- `en;English` -> `en`
- `English;it;en` -> `en;it`
- `sl;English` -> `en;sl`
- `da;English` -> `da;en`

### 2. Duplicate and taxonomy substance did not move

The cycle did not change:

- the canonical denominator (`34 / 37 / 27`);
- the extended reviewed local surface (`35 / 38 / 28`);
- any cluster-status verdict;
- any same-event merge claim;
- any taxonomy label count;
- any graph-eligible edge or graph materialization count.

### 3. The repair targets later grouping risk directly

The highest-value impact is on consumers that group by language scope while also carrying multistate duplicate controls. Those consumers no longer need a sidecar-only rule to avoid splitting `English` from `en` inside the same duplicate-control package.

## Paper-readiness delta

- The canonical duplicate ledger now carries normalized language grouping directly.
- Reviewer-safe methods prose can distinguish substantive duplicate governance from export-format drift more cleanly.
- Dashboard and appendix consumers can reuse `language_scope` without extra normalization logic.

## Claims verified, rewritten, or dropped

Verified:

- the duplicate/taxonomy denominator remains `34 / 37 / 27` core and `35 / 38 / 28` extended;
- the only substantive change is language-scope normalization;
- the normalized live language-scope surface is now `da, da;en, en, en;it, en;sl, et, fi, lt, sl`.

Rewritten:

- duplicate/taxonomy status notes now treat mixed language strings as resolved canonical formatting rather than only as a caution sidecar.

Dropped or explicitly not made:

- no new same-event duplicate claim;
- no new taxonomy label claim;
- no claim that language normalization changes legal, policy, or source-authority interpretation.

## Remaining blocker

The next real gain is still one of:

1. a join-safe same-event package across at least two evidence families;
2. an explicit sidecar-admission decision for Rytr or Air AI on the extended reviewed local surface;
3. a second reviewed lineage that lifts the MCP / authorization-abuse / research-integrity singleton frontier beyond watcher status; or
4. a direct-authority upgrade for `REVIEW-SINGLETON-LIMEN-008-LEAD-007`.

## Suggested next route

Hold the duplicate verdicts and taxonomy counts unless one of the blocker conditions above changes. If another small hygiene cycle is needed before that, inspect whether any other canonical export-facing LIMEN file still mixes code and name forms in grouping fields that already have a settled normalization rule.
