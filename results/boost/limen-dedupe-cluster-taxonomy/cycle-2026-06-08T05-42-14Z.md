# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-08T05:42:14Z
Lane id: `limen-dedupe-cluster-taxonomy`
Project root: `/srv/tyche/projects/limen-ai-edge-case-atlas`

## Why this route

The previous duplicate/taxonomy cycle left one explicit blocker unresolved: Rytr and Air AI were fully document-backed on this host, but duplicate-governance surfaces still treated them as intentional gaps pending a synchronized sidecar decision. Under the hostile-reviewer sprint, the next smallest publishability move was to resolve that governance ambiguity without inflating the seeded article denominator.

## Artifacts created or refreshed

Created:

- `results/boost/limen-dedupe-cluster-taxonomy/ftc-held-sidecar-governance-decision-v0.1.tsv`
- `results/boost/limen-dedupe-cluster-taxonomy/cycle-2026-06-08T05-42-14Z.md`

Refreshed:

- `results/clusters/duplicate-clusters-v0.1.tsv`
- `results/clusters/status.md`
- `results/taxonomy/taxonomy-delta-v0.1.md`
- `results/boost/limen-dedupe-cluster-taxonomy/live-duplicate-governance-audit-v0.1.tsv`
- `results/boost/limen-dedupe-cluster-taxonomy/status.md`

## What changed

### 1. Two live authoritative rows are now inside duplicate governance

Added ledger rows:

- `REVIEW-SIDECAR-LIMEN-000013`
- `REVIEW-SIDECAR-LIMEN-000014`

Both are governed as `no_cluster_review_row` entries, but with `extended_reviewed_local_surface_only_never_seeded_headline_counts` counting rules.

### 2. The seeded headline denominator did not move

The cycle did not change:

- the core seeded denominator (`34 / 37 / 27`);
- any same-event merge claim;
- any reviewed-not-duplicate recurrence package;
- any taxonomy headline count;
- any graph-eligible edge or graph materialization count.

The cycle did change:

- total review rows from `35` to `37`;
- extended governed record refs from `38` to `40`;
- extended unique lineages from `28` to `30`;
- live duplicate-ledger coverage from `28/30` to `30/30`.

### 3. The admission is deliberately denominator-silent at article-core level

Rytr is now a governed complex-posture reversibility sidecar, not a stable current-order exemplar.

Air AI is now a governed bounded settlement sidecar, not hidden-human closure and not a default non-finance authoritative-core row.

## Paper-readiness delta

- Every live `data/cases/*` row is now represented in one duplicate-governance surface.
- Reviewer-safe methods prose can now explain why some governed rows remain extended-only without implying they were source-missing or forgotten.
- Dashboard and thesis denominator panels can show `seeded headline` versus `extended sidecar-aware` scope explicitly.

## Claims verified, rewritten, or dropped

Verified:

- the seeded denominator remains `34 / 37 / 27`;
- the extended reviewed local surface is now `37 / 40 / 30`;
- all `30` live `data/cases/*` refs are covered by the duplicate ledger;
- the only structural change is explicit sidecar admission for Rytr and Air AI.

Rewritten:

- duplicate/taxonomy status notes now treat Rytr and Air AI as governed extended-only sidecars rather than as intentional outside-ledger gaps.

Dropped or explicitly not made:

- no new same-event duplicate claim;
- no new taxonomy label claim;
- no claim that Rytr or Air AI belong in seeded headline recurrence counts.

## Remaining blocker

The next real gain is still one of:

1. a join-safe same-event package across at least two evidence families;
2. a second reviewed lineage that lifts the MCP / authorization-abuse / research-integrity singleton frontier beyond watcher status;
3. a direct-authority upgrade for `REVIEW-SINGLETON-LIMEN-008-LEAD-007`; or
4. a later shared-consumer drift repair if the seeded-versus-extended split is flattened again.

## Suggested next route

Hold the new sidecar decision unless later project-level claim routing changes. Count further progress only if the blocker conditions above change state or if another shared consumer starts narrating the FTC sidecars as if they were seeded headline rows.
