# LIMEN dedupe / cluster / taxonomy cycle note

Cycle timestamp (UTC): 2026-06-08T06:19:20Z
Lane id: `limen-dedupe-cluster-taxonomy`
Project root: `/srv/tyche/projects/limen-ai-edge-case-atlas`

## Why this route

A new authoritative French CNIL row (`LIMEN-000015`) had entered the live atlas, but the duplicate ledger and taxonomy/status surfaces were still frozen at the earlier `30`-row contract. Under the hostile-reviewer sprint, the next smallest publishability move was to restore denominator truthfulness by admitting the row into duplicate governance without inventing a recurrence package or overstating the source depth.

## Artifacts created or refreshed

Created:

- `results/boost/limen-dedupe-cluster-taxonomy/cycle-2026-06-08T06-19-20Z.md`

Refreshed:

- `results/clusters/duplicate-clusters-v0.1.tsv`
- `results/clusters/status.md`
- `results/taxonomy/taxonomy-delta-v0.1.md`
- `results/boost/limen-dedupe-cluster-taxonomy/live-duplicate-governance-audit-v0.1.tsv`
- `results/boost/limen-dedupe-cluster-taxonomy/status.md`

## What changed

### 1. The French CNIL row is now inside duplicate governance

Added ledger row:

- `REVIEW-SINGLETON-LIMEN-000015`

It is governed as a core `no_cluster_review_row` singleton with `count_as_singleton_lineage_pending_future_overlap_review` counting rules.

### 2. The seeded denominator moved because this is a core singleton, not a sidecar

The cycle did not change:

- any same-event merge claim;
- any reviewed-not-duplicate recurrence package;
- any graph-eligible edge or graph materialization count;
- any taxonomy label inventory beyond the bounded inclusion of the French singleton's existing labels.

The cycle did change:

- total review rows from `37` to `38`;
- core governed record refs from `37` to `38`;
- core unique lineages from `27` to `28`;
- extended governed record refs from `40` to `41`;
- extended unique lineages from `30` to `31`;
- live duplicate-ledger coverage from `30/30` to `31/31`.

### 3. The new row remains bounded

`LIMEN-000015` is now governed, but only as a warning-page-depth singleton. It is not a same-event duplicate, not a recurrence package, not a sanction-depth exemplar, and not proof that the proposed facial-recognition system was actually deployed.

## Paper-readiness delta

- Every live `data/cases/*` row is again represented in one duplicate-governance surface.
- Reviewer-safe methods prose can now explain the French CNIL row as part of the governed atlas rather than as a recent authoritative add-on outside denominator control.
- Dashboard and thesis denominator panels can keep the seeded-versus-extended split truthful while preserving official-search and companion-link caution around this row.

## Claims verified, rewritten, or dropped

Verified:

- the live case inventory is now `31` rows (`15` authoritative, `10` security, `6` multilingual);
- the core seeded duplicate/taxonomy denominator is now `38 / 38 / 28`;
- the extended reviewed local surface is now `38 / 41 / 31`;
- all `31` live `data/cases/*` refs are covered by the duplicate ledger.

Rewritten:

- duplicate/taxonomy status notes now treat `LIMEN-000015` as a governed core singleton rather than leaving the live authoritative inventory ahead of the duplicate-control bundle.

Dropped or explicitly not made:

- no new same-event duplicate claim;
- no new taxonomy label claim;
- no claim that the CNIL warning page proves deployment, sanction, or a broader recurrence family.

## Remaining blocker

The next real gain is still one of:

1. a join-safe same-event package across at least two evidence families;
2. a second reviewed lineage that lifts the MCP / authorization-abuse / research-integrity singleton frontier beyond watcher status;
3. a direct-authority upgrade for `REVIEW-SINGLETON-LIMEN-008-LEAD-007`; or
4. a later shared-consumer drift repair if denominator wording falls behind again.

## Suggested next route

Hold the new singleton decision unless a stronger companion surface, a reviewed recurrence match, or a shared-consumer drift event changes state. Count further progress only if one of those threshold-bearing conditions appears.
