# LIMEN dedupe / cluster / taxonomy cycle note

Timestamp (UTC): 2026-06-08T10:25:12Z
Lane id: `limen-dedupe-cluster-taxonomy`

## Why this cycle

The canonical duplicate/taxonomy package already claimed full live-case coverage, but the proof path still depended on readers implicitly knowing that multilingual rows use `candidate_id` while authoritative and security rows use `case_id`. That is a join-safety and reviewer-truthfulness risk for duplicate-governance consumers.

## Durable artifact created

- `results/boost/limen-dedupe-cluster-taxonomy/live-record-key-coverage-audit-v0.1.tsv`

## What the validator shows

- `32 / 32` live `data/cases` records are covered by the canonical duplicate ledger.
- Record-key split is explicit rather than implicit:
  - `26` rows use `case_id`
  - `6` rows use `candidate_id`
- No uncovered live record references remain.
- The audit is file-qualified and therefore safe against bare-ID collisions already documented in `results/clusters/duplicate-clusters-v0.1.tsv`.

## Paper-readiness delta

This gives reviewer-facing methods, dashboard consumers, and thesis prose one machine-readable proof that "all live rows are governed" means file-qualified live record references across mixed key schemes, not only bare `case_id` joins.

## Claims kept conservative

- No duplicate verdict changed.
- No taxonomy label changed.
- No graph edge changed.
- No new same-event cluster was asserted.

## Remaining blocker

The next substantive lane gain still needs one join-safe same-event package, one non-Springer direct notice or institutional research-integrity action that changes the direct-notice concentration ceiling, one direct-authority upgrade for `REVIEW-SINGLETON-LIMEN-008-LEAD-007`, or one new non-MCP trust-boundary lineage that changes the current asymmetry.
