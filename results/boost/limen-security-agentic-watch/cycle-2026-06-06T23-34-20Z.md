# Security mitigation visibility note — 2026-06-06T23:34:20Z

Lane: `limen-security-agentic-watch`
Project: `limen-ai-edge-case-atlas`
Input package: `data/cases/security-agentic-candidates.jsonl`, `results/security/security-agentic-watch-v0.1.tsv`
Derived artifact: `results/security/security-agentic-mitigation-visibility-v0.1.tsv`

## Why this cycle

The hostile-reviewer sprint favors package hardening over raw case growth. The most efficient next move inside the existing seed package was to convert five public CVE-style rows into a reviewer-readable mitigation-visibility matrix rather than add more weakly verified cases.

## Paper-readiness delta

This cycle turns the seed watch into a table-ready result about remediation visibility:

- 2 of 5 rows currently name a fix version in the captured public source text;
- 1 of 5 rows currently names a workaround;
- 0 of 5 rows currently include a separately captured vendor notice in the local package;
- execution-boundary rows show the widest spread, from `mitigation_visibility_score=3` (Auto-GPT) to `0` (Langflow candidate);
- both CrewAI rows remain useful descriptive evidence, but mitigation visibility is weak enough that they should stay in a verification queue for stronger comparative claims.

## Figure/table hook

Recommended first security figure/table: `surface_family -> impact_mode -> mitigation_visibility_score`.

Suggested dashboard interpretation:
- show that public security evidence already supports a structured agent-surface taxonomy;
- simultaneously show that remediation visibility is uneven and therefore constrains comparative safety claims.

## Claim boundary

This artifact supports only a bounded descriptive claim: in the current five-row local security seed, public-source visibility of mitigations is uneven across otherwise comparable agentic-security surfaces.

It does not support claims about real-world exploit frequency, vendor responsiveness in general, or overall framework security quality.

## Remaining blocker

The package still lacks separately captured issuer or vendor notices for all five rows. The next high-value move is a metadata-only company/platform notice subledger that records whether an authoritative remediation notice exists, its publication date, and whether it changes evidence tier or mitigation interpretation.
