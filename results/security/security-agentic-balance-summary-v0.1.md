# Security agentic balance summary v0.1

As of 2026-06-07T15:49:38Z, the shared security/agentic package contains 10 local rows. This summary converts the live watch TSV into a reviewer-safe balance register for the first LIMEN article package and dashboard.

## What the package can now show

- 4 rows (`LIMEN-000001`, `LIMEN-000003`, `LIMEN-000008`, `LIMEN-000009`) now combine either a primary CVE-style record plus vendor-authored advisory-grade remediation language or a direct upstream advisory with exact patched-version visibility.
- 1 row (`LIMEN-000012`) now adds a distinct vulnerability-record-plus-vendor-release-note authority shape for an MCP/browser-control trust boundary, which is strong enough to remove the prior supply-chain/plugin zero state while still staying below advisory-grade certainty.
- 2 rows (`LIMEN-000004`, `LIMEN-000005`) still show a middle notice layer: public coordination evidence plus relevant vendor release-note signals, but no CVE-specific vendor bulletin.
- 2 rows (`LIMEN-000006`, `LIMEN-000007`) provide explicit independent-lab public-security support, which is enough for a bounded source-independence discussion but not for a balanced-family claim.
- 1 row (`LIMEN-000008`) now gives the family a vendor-backed memory/state contamination exemplar instead of an execution-only shape.
- Execution-boundary rows remain the dominant visible mechanism class (`LIMEN-000001`, `LIMEN-000002`, `LIMEN-000006`, `LIMEN-000007`).
- Exact local AVID interoperability remains singleton and currently attaches only to the weakest remediation-notice row (`LIMEN-000002`).

## Gaps that still cap manuscript rhetoric

- No peer-reviewed security case support is present in the current local package.
- Identity-abuse / agent-identity coverage is no longer zero, but it is still singleton and vendor-advisory-centric (`LIMEN-000009`).
- Supply-chain / plugin / dependency-trust coverage is no longer zero, but it is still singleton and release-note-qualified through `LIMEN-000012`.
- The package remains framework-heavy even after the LangChain memory/state addition and the new PraisonAI identity-boundary row.

## Paper/thesis use

Best immediate use: a methods/results table or dashboard heatmap that separates authority shape, mechanism concentration, and explicit gap states without overreading them as prevalence. The new machine-readable file `results/security/security-agentic-balance-gap-matrix-v0.1.tsv` is suitable for a reviewer-facing appendix, a blocker overlay in the source-family saturation figure, or a dashboard facet keyed by `dimension_type`, `dimension_key`, `coverage_state`, and `covered_case_ids`.

## Strongest safe claim

LIMEN can now say that the current public-source security package is heterogeneous across notice depth and mechanism shape: vendor-advisory-backed, vulnerability-record-plus-release-note, independent-lab, memory/state, identity-boundary, and one MCP trust-boundary row all exist, yet peer-reviewed support remains absent and both identity and supply-chain coverage remain singleton.

## Next smallest publishability move

Add one incident-specific peer-reviewed public-security exemplar or one second equally well-bounded non-framework trust-boundary row before broadening any family-balance language; identity-abuse and supply-chain coverage are no longer zero but both still need second exemplars before stronger comparative use.
